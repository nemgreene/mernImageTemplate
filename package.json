{
  "name": "mernimagetemplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"cd client && npm run start\" \"cd server && nodemon index.js\" ",
    "install": "concurrently \"cd client && npm install\" \"cd server && npm install\" "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.1.0"
  }
}
